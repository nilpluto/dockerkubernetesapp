kind: pipeline
type: docker
name: build
steps:

  - name: build
    image: library/openjdk:11-jdk
    commands:
      - ./gradlew clean build


  - name: docker-build-publish
    image: plugins/docker
    dockerfile: dockerfile
    settings:
      repo: just-a-coder/dockerkubernetesapp
      auto_tag: true
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password

  - name: kubernetes-deploy
    image: brianberzins/kubectl:latest
    commands:
      - echo "Line1"
      - echo pwd
      - echo ls
      - kubectl get pods
      - echo "Line2a"
      - kubectl apply --filename src/kubernetes/deployment.yaml
      - echo "Line2"
      - kubectl apply --filename src/kubernetes/service.yaml
      - echo "Line3"
      - kubectl apply --filename src/kubernetes/ingress.yaml
      - echo "Line4"