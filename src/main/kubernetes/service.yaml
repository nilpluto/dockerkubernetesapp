apiVersion: v1
kind: Service
metadata:
  labels:
    name: minikube-service
  name: minikube-service
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      nodePort: 30567
      targetPort: 8000
  selector:
    app: minikube-deploy